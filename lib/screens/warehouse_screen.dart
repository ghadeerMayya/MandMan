import 'package:flutter/material.dart';

class WareHouse extends StatelessWidget {
  static const routeName = '/warehouse';

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('WareHouse'),);
  }
}
